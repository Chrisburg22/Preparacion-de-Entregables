#include <stdio.h>
#include <stdlib.h>

int main(){
    int cantidadMaestros, cantidadAlumnos;
    cantidadMaestros=01;
    cantidadAlumnos=22;

    ((cantidadMaestros==1)&&(cantidadAlumnos>0)) ? printf("Si hay clase\n") : printf("No hay clase\n");

    return 0;
}